# Local scratch (repo root)
/.tmp/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/

# Encrypted files anywhere in the repo (DEK, sops outputs, secrets-test.enc, etc.)
*.enc
*.enc.*
*.encrypted
*.encrypted.*

# secconfig example — local sops config (contains age recipient)
secconfig/examples/.sops.yaml
