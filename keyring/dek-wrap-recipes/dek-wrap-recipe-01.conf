# util keyring — DEK wrap recipe 01 (OpenSSL enc + PBKDF2).
# Parsed by keyring/lib.bash; keys and values are allowlisted.
#
RECIPE_ID=01
HEADER_MAGIC=UTILDEK1
HEADER_FORMAT_MAJOR=1
HEADER_FORMAT_MINOR=0
OPENSSL_CIPHER=aes-256-cbc
OPENSSL_MD=sha256
PBKDF2_ITER=600000
OPENSSL_MIN_VERSION=1.1.1
OPENSSL_TESTED_WITH=3.0.13
